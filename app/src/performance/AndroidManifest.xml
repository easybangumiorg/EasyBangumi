<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:icon="@mipmap/logo_performance"
        tools:replace="android:icon">

        <!-- Non-debuggable like release, but readable by adb/Perfetto/Macrobenchmark. -->
        <profileable
            android:shell="true"
            tools:targetApi="29" />
    </application>

</manifest>
