<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:icon="@mipmap/logo_testing"
        tools:replace="android:icon">

        <!-- Match the Performance build: release-equivalent code remains observable by
             adb/Perfetto/Macrobenchmark without making the application debuggable. -->
        <profileable
            android:shell="true"
            tools:targetApi="29" />
    </application>

</manifest>
