## 1. 播放器桥接层扩展

- [x] 1.1 在 `libplayer/api` 增加桌面端音量读取、设置与静音切换接口定义
- [x] 1.2 在 `libplayer/vlcj` 完成音量接口实现，并统一做 0-100 区间钳制
- [x] 1.3 在桥接调用失败场景返回可识别状态，供上层 UI 进行降级处理

## 2. 桌面播放器状态与持久化

- [x] 2.1 在桌面播放器 VM 增加 `volume`、`mute` 状态以及统一音量 Action
- [x] 2.2 将 `Seekbar`、按钮、快捷键、滚轮入口统一接入同一音量更新链路（拖动实时预览 + 节流持久化）
- [x] 2.3 接入本地偏好存储，保存并恢复最近一次音量值（不跨会话恢复静音）

## 3. 桌面播放器 UI 与交互

- [x] 3.1 在桌面播放器控制栏常驻音量图标（点击切换静音），鼠标悬停时在图标上方展示临时音量 `Seekbar` 小窗并展示当前音量
- [x] 3.2 实现快捷键与滚轮音量步进调节，保持与控件一致的边界规则
- [x] 3.3 在底层不支持或写入失败时展示禁用/降级态，避免误导性反馈
- [x] 3.4 将音量小窗滑轨调整为桌面专用竖向样式（底部 0、顶部 100），并将百分比文本固定展示在滑轨上方

## 4. 验证与回归

- [x] 4.1 增加或补充音量状态同步与边界钳制相关单元测试
- [ ] 4.2 执行桌面端手工回归：播放、切集、全屏切换、重启恢复音量
- [x] 4.3 记录验证结果与已知限制，确保 `/opsx:apply` 阶段可直接执行

## 验证记录

- 2026-03-24：执行 `./gradlew :shared:playcon:desktopTest`，新增音量状态同步与边界钳制测试通过。
- 2026-03-24：执行 `./gradlew :shared:compileKotlinDesktop`，桌面端共享模块编译通过。

## 已知限制

- 当前 CLI 环境无法进行图形化手工回归，任务 4.2 仍待在桌面端人工验证播放、切集、全屏切换与重启恢复音量场景。
