## 1. 现状审计与领域基础

- [x] 1.1 审计请求持久化、action 链、调度器、恢复流程、下载页和完整/快速模式边界。
- [x] 1.2 为 `CartoonDownloadReq` 添加兼容历史 JSON 的快速引擎 ID，并提取可测试的任务计划。
- [x] 1.3 定义快速引擎契约、描述信息和唯一 ID 注册表。

## 2. 引擎与任务管理

- [x] 2.1 将 Aria 接入快速引擎契约并由稳定 `QuickDownloadAction` 代理。
- [x] 2.2 实现支持 HTTP Range 的 OkHttp 直链引擎、媒体能力协商，并确保直接 MP4 被后续转码阶段透传。
- [x] 2.3 实现统一 `CartoonDownloadTaskManager`，集中入队、删除、恢复、切换引擎与回退完整模式。
- [x] 2.4 修复 dispatcher 先执行后发布的竞态，并隔离陈旧 runtime 回调。

## 3. 下载列表重构

- [x] 3.1 将 ViewModel selection 和事件参数迁移为 UUID，并自动清理失效选择。
- [x] 3.2 增加任务模式、引擎、阶段和状态投影，移除 UI 对 runtime/action 的直接命令调用。
- [x] 3.3 重构列表卡片、稳定 key、空状态、批量操作和失败任务的重试/引擎选择对话框。

## 4. 验证

- [x] 4.1 添加任务计划、引擎注册表、模式/引擎切换和稳定选择的单元测试。
- [x] 4.2 运行 OpenSpec 严格校验和下载相关单元测试。
- [x] 4.3 运行 Debug Kotlin 编译或 APK 构建并修复回归。
