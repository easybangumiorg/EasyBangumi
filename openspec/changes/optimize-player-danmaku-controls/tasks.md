## 1. 显示配置基础

- [x] 1.1 核对当前 DanmakuFlameMaster 版本中滚动速度、轨道 margin 与最大行数相关 API，并用最小真机/仪器实验确定行高和用户语义速度的安全映射。
- [x] 1.2 扩展 `DanmakuDisplayPreferences`，增加字体大小、行高倍率和用户语义滚动速度偏好，保留现有键与兼容默认值。
- [x] 1.3 新增不可变 `DanmakuDisplayConfig`、默认值、有效范围和 `NaN`/无穷大/越界值归一化逻辑。
- [x] 1.4 提供统一配置读写与恢复默认操作，使播放器面板和全局播放器设置使用同一偏好来源。
- [x] 1.5 为默认值、边界归一化、持久化恢复和恢复默认行为增加单元测试。

## 2. DFM 即时重配置

- [x] 2.1 调整 `DfmDanmakuOverlay`，由 V2 播放状态所有者传入归一化显示配置，避免渲染器与控制层分别读取原始偏好。
- [x] 2.2 为 `DfmDanmakuRenderer` 增加显示配置入口和影响分类，区分可见性、类型/时间偏移与样式重建。
- [x] 2.3 移除 18sp 硬编码，让滚动、顶部和底部弹幕项目使用已归一化字体大小。
- [x] 2.4 按已验证的 DFM 映射实现 1.0–2.0 行高倍率，并保证轨道间距非负且不会越出安全范围。
- [x] 2.5 将 0.5x–2.0x 用户语义滚动速度转换为 DFM 参数，仅影响滚动弹幕，不改变固定弹幕时间语义。
- [x] 2.6 合并或限频连续滑块更新，样式重建后恢复当前 DFM 时间及播放/暂停状态，不调用 ExoPlayer Seek。
- [x] 2.7 补充 renderer policy 与适配器测试，覆盖暂停中修改、连续修改、全屏重建、Seek 后配置和旧 View 延迟回调。
- [x] 2.8 扩展大弹幕集仪器测试，在 5000 条弹幕下验证字体、行高、速度切换、资源释放和控制触摸稳定性。

## 3. 快捷开关与 V2 隔离

- [x] 3.1 定义 `PlayerDanmakuControlState` 及事件接口，从显示配置和 `DanmakuPlaybackState` 派生开启、关闭、加载与不可用状态。
- [x] 3.2 为共享 `VideoControl`/`EasyVideoBottomControl` 增加默认为空的可选弹幕能力，在播放/下一集之后、时间轴之前提供 48dp 快捷开关。
- [x] 3.3 为快捷开关实现开启、关闭、加载和不可用视觉、内容描述与触摸反馈；已有弹幕时只切换显示偏好。
- [x] 3.4 将未匹配、失败和弹幕源禁用状态分别接入现有匹配、重试或设置动作，避免静默重复请求。
- [x] 3.5 仅从 V2 播放页注入弹幕能力，保持 Legacy 调用方为 `null`，并验证旧版控制条和回退路由不受影响。
- [x] 3.6 增加 Compose UI 测试，覆盖快捷开关位置与状态、偏好切换不触发匹配、不可用动作以及 Legacy 不展示弹幕入口。

## 4. 统一 Material 3 播放设置面板

- [x] 4.1 用统一播放器设置状态替代 V2 的直接 `showScaleTypeWin` 流程，支持面板显隐和 `Danmaku`/`Video` 当前分类。
- [x] 4.2 实现自适应 Material 3 容器：横屏/宽屏使用右侧面板，紧凑竖屏使用 Bottom Sheet，并处理系统栏、遮罩、滚动和返回优先级。
- [x] 4.3 实现标题、关闭按钮和“弹幕/画面”双项分段控件，确保分类切换不暂停视频、不关闭面板或改变进度。
- [x] 4.4 抽取可复用弹幕显示设置内容，提供总开关、类型控制、字体大小、行高、滚动速度、时间偏移、当前值和确认式恢复默认。
- [x] 4.5 将可复用设置内容接入全局 `PlayerSetting`，移除与播放器面板不一致的范围或重复长期状态。
- [x] 4.6 在“画面”分类复用 `videoScaleTypeSelection`、当前选中态和 `setVideoScaleType`，以 Material 单选列表替代 V2 独立黑色拉伸侧栏。
- [x] 4.7 增加 Compose UI 测试，覆盖打开/关闭、分类切换、调节器语义与范围、恢复默认、画面选中态和两个设置入口同步。

## 5. 集成验证与回退

- [x] 5.1 运行完整 JVM 单元测试、Compose UI 测试、Debug 构建与 AndroidTest 构建，修复本变更引入的回归。
- [x] 5.2 在连接设备上验证普通屏/全屏快捷开关、暂停中改样式、连续拖动、Seek、全屏切换和页面释放均不重播旧弹幕或改变视频进度。
- [x] 5.3 在 320dp、360dp、约 411dp、横屏和平板宽度及大字体模式下验证底部控制条、侧栏/Bottom Sheet 和所有配置项可触达无溢出。
- [x] 5.4 验证 5000 条弹幕下字体 12–36sp、行高 1.0–2.0、速度 0.5x–2.0x 的边界表现、帧稳定性和固定弹幕不受速度影响。
- [x] 5.5 验证画面 7 种模式仍可即时切换并正确持久化，播放源、选集、弹幕 binding 和播放进度保持不变。
- [x] 5.6 记录 V2 启用结果、Legacy 隔离结果和回退步骤；出现阻断问题时可隐藏新入口并恢复 V2 原画面比例侧栏，无需回滚弹幕数据。

## 6. 全屏弹幕连续性回归修复

- [x] 6.1 使用 ADB 对比横竖屏两侧的 Compose、视频和弹幕 View identity，并量化方向变化首帧耗时，确认原生 `DanmakuView` 被 holder 重建而视频 View 被复用。
- [x] 6.2 将 DFM renderer 和原生 View 所有权提升到 V2 页面级，同一 Activity Context 下跨 holder 重新挂载同一个 prepared View，并将异步 prepared 状态变更串行到主线程。
- [x] 6.3 增加原生 View identity、prepared 连续性、不同 Context 重建和最终 release 的仪器回归测试。
- [x] 6.4 运行 JVM、AndroidTest 编译和 Debug 构建，在连接设备上验证全屏往返后 View identity、弹幕连续性、播放进度和退出释放。
